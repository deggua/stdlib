#pragma once

#include <deggua/units.h>
#include <deggua/bitops.h>
#include <deggua/logging.h>
#include <deggua/macros.h>
#include <deggua/types.h>

// TODO: lets just write my own stdlib
// e.g. everything I would want in a C stdlib
// * proper strings and string functions
// * circular buffers (FIFOs, virtual and non-virtual)
// * Memory arenas
// * BitVector
// * Generics (Vector, StableVector, Map, StableMap, Set, StableSet, List)
// * Vector, StaticVector (virtual/non-virtual), BitVector
// * Coroutines
// * Maybe(T)
// * PRNG (PCG, LCG, MCG)
// * Floating point functions
// * Type safe printf
// * Mutlithreading and concurrency (threads, semaphores, mutexes)
// * Vector math, matrix math
// * Date and time
// * Benchmarking/profiling utilities
// * More bitmanip functions (see C23 header, alignto, etc.)
